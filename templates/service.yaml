apiVersion: v1
kind: Service
metadata:
  name: {{ include "seckill.fullname" . }}
  labels:
    {{- include "seckill.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seckill.selectorLabels" . | nindent 4 }}
