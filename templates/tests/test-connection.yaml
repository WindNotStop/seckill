apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seckill.fullname" . }}-test-connection"
  labels:
    {{- include "seckill.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seckill.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
